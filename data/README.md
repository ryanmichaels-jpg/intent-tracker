# data/ — never committed

`data/raw/` holds downloaded scrapes, `data/out/` holds normalized batches.
Both are gitignored. This keeps all personal data out of the public repo.
Put a CRM-free `master_export.csv` here if you want cross-week REPEAT tagging.
